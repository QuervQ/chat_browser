{
  "name": "browser_procla",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix frontend",
    "build": "npm run build --prefix frontend"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.87.0"
  }
}
